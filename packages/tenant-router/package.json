{
  "name": "@operate/tenant-router",
  "version": "0.0.1",
  "private": true,
  "description": "Tenant resolution + per-request Prisma client factory. Reads subdomain (or /t/{slug} fallback) from the request and yields the right Prisma client.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@operate/db-control": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
